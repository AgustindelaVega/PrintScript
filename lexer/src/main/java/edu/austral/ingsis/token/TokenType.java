package edu.austral.ingsis.token;

public enum TokenType {
    LET,
    NUMBER,
    STRING,
    SEMICOLON,
    COLON,
    ASSIGNATION,
    PRINT,
    MINUS,
    PLUS,
    MULTIPLY,
    DIVIDE,
    IDENTIFIER,
    NUMBER_TYPE,
    STRING_TYPE,
    EOF,
}
