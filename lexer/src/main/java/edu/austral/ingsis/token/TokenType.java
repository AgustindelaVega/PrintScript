package edu.austral.ingsis.token;

public enum TokenType {
    LET,
    NUMBER,
    NUMBERTYPE,
    STRING,
    STRINGTYPE,
    SEMICOLON,
    COLON,
    ASSIGNATION,
    PRINT,
    MINUS,
    PLUS,
    MULTIPLY,
    DIVIDE,
    IDENTIFIER,
    EOF,
    NEWLINE,
}
